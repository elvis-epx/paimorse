FIXMEs

GUI
setup.py install
man page for cw

SAMPLING em osxca

Mac: playing slower than Linux (25 wps is actually playing 20 wps)
	(currently worked aroung with an empirical compensation
	 factor, still sounds better than Linux :P)

Irc:
    # hardcoded server/port
    # save nickname etc in a separate config
    # allow to talk, too
