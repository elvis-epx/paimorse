GUI
setup.py install
man page for cw
enforce sampling rate em osxca
osxca: buffer multiple of coreaudio buffer

Irc:
    # hardcoded server/port
    # save nickname etc in a separate config
    # allow to talk, too
